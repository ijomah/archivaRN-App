export default class FileManDataShape {
    constructor(valueId, label, count) {
        this.value = valueId;
        this.docTitle = label;
        this.fileCount = count;
        // this.img = imgUri;
    }
}